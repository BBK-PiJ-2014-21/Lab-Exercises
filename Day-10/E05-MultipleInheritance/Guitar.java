/* 5. Multiple inheritance
	java does not allow to extend more than one class;
	a workaround could be to create two default methods in two interfaces implemented by Guitar class
*/

public class Guitar implements MusicalInstrument, WoodenObject {
	
}

